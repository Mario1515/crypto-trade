<main>
    <section id="details-info">
        <h1>Details</h1>
        <div class="coin-image">
            <img src="{{crypto.image}}">
        </div>

        <div class="coin-info">
            <div class="coin-text">
                <h1 id="name">{{crypto.name}}</h1>
                <h3 id="payment">{{crypto.payment}}</h3>
                <p id="price"><span>Price: ${{crypto.price}}</span></p>
                <p id="description">{{crypto.description}}</p>
            </div>
            <div class="product-btn">

                {{#if isAuthenticated}}
                {{#unless isOwner}}
                {{#unless isBuyer}}
                <form method="POST" action="/crypto/{{crypto._id}}/buy">
                    <button type="submit" class="btn-buy">Buy</button>
                </form>
                {{/unless}}
                {{/unless}}
                {{/if}}
                <!-- Logged in user who has already buy this crypto-->
                {{#if isAuthenticated}}
                {{#if isOwner}}
                <p class="buy-message">You already bought these crypto coins.</p>
                <div class="author">
                    <a href="/crypto/{{crypto._id}}/edit" class="btn-edit">Edit</a>
                    <a href="/crypto/{{crypto._id}}/delete" class="btn-delete">Delete</a>
                </div>
                {{/if}}
                {{/if}}
                {{#if isAuthenticated}}
                {{#if isBuyer}}
                <p class="buy-message">You already bought these crypto coins.</p>
                {{/if}}
                {{/if}}
            </div>
        </div>
    </section>
</main>